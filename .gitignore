# --------------------------------------------------------------------
# [기본 설정] Python & ROS2 빌드 아티팩트
# --------------------------------------------------------------------
build/
install/
log/
*.log

# Python 컴파일 및 가상환경
__pycache__/
*.py[cod]
*$py.class
.venv/
env/

# IDE 설정 파일
.vscode/
.idea/

# --------------------------------------------------------------------
# [사용자 요청] 학습 데이터, 이미지, 결과물 제외 설정
# --------------------------------------------------------------------

# 1. 대용량 데이터셋 폴더
train_data/
train_data.zip
captures/

# 2. 캘리브레이션 및 호모그래피 데이터 (이미지 포함된 것으로 확인됨)
homography_data/
homography_data_640/
pt_data/
csv_data/

# 3. 결과물 폴더 (비교 결과, 신뢰도 결과 등)
comparison_result/
confidence_result/
resource/

# 4. 모델 가중치 파일 (YOLO 등 .pt 파일은 용량이 커서 Git 제외 권장)
models/

# 5. 실행 로그 및 임시 폴더 (날짜형식 폴더 패턴 포함)
*_detection-*/
subway_log.db

# 6. 이미지 파일 및 그래프 결과물
# (코드 실행 시 생성되는 그래프나 캡처본)
*.jpg
*.jpeg
*.png
comparison_*.png

# 7. 압축 파일 (백업본)
*.zip

# --------------------------------------------------------------------
# [예외 처리] 만약 특정 이미지는 꼭 올려야 한다면?
# --------------------------------------------------------------------
# 예: 아이콘이나 리드미용 이미지는 예외로 허용하고 싶다면 아래 주석 해제
# !assets/
# !README.png